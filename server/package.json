{
  "name": "project-connect-server",
  "version": "1.0.0",
  "type": "module",
  "main": "src/app.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "dev": "node --watch src/app.js",
    "start": "node src/app.js",
    "build": "echo 'No build step required for Node.js backend'",
    "migrate": "psql \"$DATABASE_URL\" -f ./sql/init.sql",
    "postinstall": "echo 'Backend dependencies installed'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.5"
  }
}